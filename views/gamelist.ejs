    
     <%- include('partials/head.ejs') %>
     
       <div class="container">
  <div class="card-body" >
         <% for(var i=0; i<gamelist.length;i++){ %>
     <div class = "container"  >
         <h5 class="card-title">game:<%=gamelist[i].title %></h5>
           <h6 class="card-title">creator:<%=gamelist[i].creator%></h6>
            <h6 class="card-title">type:<%=gamelist[i].type%></h6>
           <a href="/edit/<%= gamelist[i].id %>" class="btn btn-primary">Edit</a>
           <a href="/del/<%= gamelist[i].id %>" class="btn btn-primary">del</a>
           </div>
      <%  }%>
    
        
      </div>
  </div>
</div>
       
       
   
    
    </body>
</html>